{
  "id": "cond.update-apply",
  "category": "condition",
  "label": "scholia update に --check が指定されていない（実更新を行う）とき",
  "tags": [
    "axis.update.mode"
  ],
  "description": "cond.update-check-flag の complement。axis.update.mode の2値目で、`--check` でない場合の実更新（実際にバイナリを置換する経路）を表す。"
}
